# Device Configuration (.env.example)
# Copy this file to .env and fill in your actual values
# .env is gitignored and will not be committed to the repository

# OTA Configuration
OTA_PASSWORD=your-ota-password

# Device IP Addresses for OTA uploads
ESP32DEV_IP=192.168.x.x
ESP8266_IP=192.168.x.x
ESP32_SMALL_GARAGE_IP=192.168.x.x

# MQTT Broker
MQTT_BROKER_IP=192.168.x.x
MQTT_BROKER_PORT=1883

# WiFi Network (optional - devices use WiFiManager portal if not set)
WIFI_SSID=your-ssid
WIFI_PASSWORD=your-password

# Notes:
# - OTA_PASSWORD: Used for over-the-air firmware updates (--auth flag in platformio.ini)
# - Device IPs: Static IPs or DHCP reservations for reliable OTA uploads
# - MQTT Broker: IP address of your MQTT broker
# - WiFi credentials: Optional - WiFiManager will prompt if not configured
