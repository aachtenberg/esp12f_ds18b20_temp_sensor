/**
 * BME280 Sensor - Secrets Configuration
 * 
 * SECURITY WARNING:
 * ================
 * DO NOT commit secrets.h to version control!
 * DO commit secrets.h.example as a template.
 *
 * Setup Instructions:
 * 1. Copy this file to secrets.h: cp secrets.h.example secrets.h
 * 2. Update all values for your MQTT broker and OTA password
 * 3. Never commit secrets.h
 */

#ifndef SECRETS_H
#define SECRETS_H

// =============================================================================
// MQTT BROKER CONFIGURATION
// =============================================================================

#define MQTT_SERVER "192.168.1.100"  // MQTT broker IP address
#define MQTT_PORT 1883               // MQTT port (typically 1883)
#define MQTT_USER "mqtt_user"        // MQTT username (or empty string if no auth)
#define MQTT_PASSWORD "mqtt_password"    // MQTT password (or empty string if no auth)

// =============================================================================
// OTA UPDATE PASSWORD
// =============================================================================
// Used for secure over-the-air firmware updates
// Must match the password used in your OTA upload commands

#define OTA_PASSWORD "YOUR_OTA_PASSWORD"

#endif // SECRETS_H
